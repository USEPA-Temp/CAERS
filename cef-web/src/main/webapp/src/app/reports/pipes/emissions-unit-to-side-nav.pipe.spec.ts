import { EmissionsUnitToSideNavPipe } from './emissions-unit-to-side-nav.pipe';

describe('EmissionsUnitToSideNavPipe', () => {
  it('create an instance', () => {
    const pipe = new EmissionsUnitToSideNavPipe();
    expect(pipe).toBeTruthy();
  });
});
