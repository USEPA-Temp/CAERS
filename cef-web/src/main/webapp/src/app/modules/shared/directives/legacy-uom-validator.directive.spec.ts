import { LegacyUomValidatorDirective } from './legacy-uom-validator.directive';

describe('LegacyUomValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new LegacyUomValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
