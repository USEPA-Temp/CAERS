import { WholeNumberValidatorDirective } from './whole-number-validator.directive';

describe('WholeNumberValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new WholeNumberValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
