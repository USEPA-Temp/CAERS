import { LegacyItemValidatorDirective } from './legacy-item-validator.directive';

describe('LegacyItemValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new LegacyItemValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
